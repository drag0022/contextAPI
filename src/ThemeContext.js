import { createContext } from 'react';

const ThemeContext = createContext();
//just create a Context object that we can use in our app
//we can have many of these files if we need
//or the object we put into this context can be very complex

export default ThemeContext;
